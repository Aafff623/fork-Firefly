# 归档（archive）

- 合并到 `master`/`main` 的特性分支：将 `docs/outputs/commit-history/{branch}/` 移入 `commit-history/archive/`。
- 关闭的 Issue：在 `.scratch/<feature>/` 文件顶部标记 `Status: done` 或移入 `.scratch/archive/`。
- 不要删 git 历史；归档只整理**可读摘要**与 scratch。
