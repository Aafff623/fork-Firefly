# tta-tone / extract / output（2026-08-15）

## 已落地

- tta-tone 真源：`D:\OneDrive\Desktop\tta\tta-tone\skills\tta-tone`
- 成稿跟 `references/canon.md`；词表 `references/voice/` 只读；工程短卡仍是 `references/kb/`
- 用语写入在 `knowledge-output`（`references/voice-ingest.md`），不写进 tta-tone Skill
- Knowledge 换根、output 改扫 Obsidian：**还没做**。理想稿以园主调完的 Obsidian 为准

## 三段

```text
extract  求全 → 问现场 → 配图 → 停
园主     在 Obsidian 调到理想
output   自检 → 封面 → 落盘 → 用语进库（默认自动；也可只抽指定节/词）
```

RSS 不绑新人格，不抽用语。

## 未做

- Knowledge 换根到 vault
- output 脚本改扫 Obsidian
- 讲书人格
- Wiki / Release（未点名不发）
