# tta-tone / extract / output（2026-08-15；落盘口径 2026-08-16 修订）

## 已落地

- tta-tone 真源：`D:\OneDrive\Desktop\tta\tta-tone\skills\tta-tone`
- 成稿跟 `references/canon.md`；词表 `references/voice/` 只读；工程短卡仍是 `references/kb/`
- 用语写入在 `knowledge-output`（`references/voice-ingest.md`），不写进 tta-tone Skill
- **extract 落盘 = vault**（`D:\OneDrive\Desktop\Notes\threetwoa_ob`）。渠道 1 写回该笔记所在目录；渠道 2–3 写入已有主题夹（优先 `Agentic Coding/`）。禁止把「写到 Obsidian」理解成 Knowledge
- 理想稿以园主调完的 Obsidian 为准。output 优先读 vault 笔记；`Knowledge/todo` 只读旧库存

## 三段

```text
extract  求全 → 问现场 → 配图 → 停（写入 threetwoa_ob）
园主     在 Obsidian 调到理想
output   自检 → 封面 → 落盘 → 用语进库（默认自动；也可只抽指定节/词）
```

RSS 不绑新人格，不抽用语。

## 未做

- output 脚本改扫整个 vault（另一场大活；扫描根仍是旧 `Knowledge/todo`）
- 旧 Knowledge 整库迁进 vault
- 讲书人格
- Wiki / Release（未点名不发）
