# 进料五段（暂定）

2026-08-15 园主点头暂定。RSS 不绑新人格。Knowledge 换根还没做，本页只锁顺序。

1. **求全。** 尽量收集客观信息，先写入 Obsidian，不先过滤。
2. **问现场。** 像 ZCode 那样逐项记、逐项问。用户不补就停，不准编。
3. **配图。** 用户补图，或检索场景图插入 vault。封面 MiniMax 留到发布。
4. **挑选。** 微调得差不多之后，用默认 tta-tone 挑选并呈现。客观层不覆盖。
5. **发布。** 再微调；没问题了做封面，再走 output 现有发布。

output 自检不过：只回跳缺的那一段，不整段重跑 extract。
