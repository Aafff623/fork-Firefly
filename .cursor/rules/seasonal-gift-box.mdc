---
description: 特定节日时主动提醒左侧公告「520惊喜礼盒」；改礼盒时遵守交互约定
alwaysApply: true
---

# 节日惊喜礼盒（Agent 提醒）

## 何时主动提起

与园主对话时，若**今天接近或正值**下列节日（前后各 2 天也算），且话题涉及站点 UI、侧栏、公告、访客体验、季节运营时，**主动提一句**：左栏公告「不期而至」（`#announcement` → `.gift-surprise`）是不定期惊喜信封，悬停晃动邀请，点击后开盖并全屏放大信封供阅读。

| 节日 | 日期（每年） |
|---|---|
| 520 / 521 | 05-20、05-21 |
| 七夕 | 农历七月初七（若不确定当年公历，只提「临近七夕」） |
| 情人节 | 02-14 |
| 圣诞节 / 元旦 | 12-25、01-01 |
| 站庆 / 上线日 | 见 `siteConfig.siteStartDate`（同月同日） |

勿每条消息都提；同一次对话提一次即可。非节日相关话题不必强行插入。

## 交互约定（改 UI 时遵守）

1. **常驻**：3D 礼盒缓慢自转（不晃动）
2. **悬停**：同形态 3D 轻晃邀请（**不**切到酒红包装盒）
3. **点击**：confetti → 包装盒淡入并快速拆开 → 全屏信封 → 点「我已阅读」（或 Esc）→ 信封收起 → tsParticles → 侧栏卡高度同步收合（记 `gone`）

实现入口：`Announcement.astro` + `src/components/widget/gift/*` + `gift-surprise.css`。  
换新惊喜：bump `announcementConfig.loveLetter.version`（或改 heading/message）；访客「已开启」/收卡标记按期号存 localStorage，新年份自动消失。
